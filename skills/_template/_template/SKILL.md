---
name: skill-name
description: One or two sentences stating WHAT this skill does AND WHEN to use it. Lean slightly pushy so it triggers when relevant, e.g. "Use this whenever the user mentions X, Y, or needs Z, even if they don't say the word 'skill'."
# compatibility: optional - required tools or dependencies, rarely needed
---

# Skill Name

One line on what this enables.

## When to use
- Concrete trigger situations (mirror the description).
- When NOT to use, if there's an easy confusion.

## Steps
1. The actual procedure, in order.
2. Keep this body focused (under ~500 lines).
3. For deep detail, point to a file in `references/` and say when to read it.
4. For deterministic/repetitive work, put code in `scripts/` so it runs without
   loading into context.

## Resources (optional)
- `references/` - docs loaded as needed
- `scripts/`    - executable code
- `assets/`     - templates, icons, fonts used in output
