# MCP map — this project

Active MCPs for this project. Global MCP config: ~/.cursor/mcp.json.

| MCP | Status | Used by |
|-----|--------|---------|
| agentmemory | required | all roles |
| github | required | researcher, builder, reviewer |
| playwright | required | builder, reviewer |
| brave-search | required | researcher |
| filesystem | required | all roles |
| ssh-vm | optional | builder (if using VM) |
| computer-use | optional | builder, reviewer |
